<!DOCTYPE html>
<html lang="sv">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Lägg till kurs</title>
</head>

<body>
    <header>
        <nav>
            <ul>
                <li><a href="/">Hem</a></li>
                <li><a href="/newcourse" class="current-page">Lägg till kurs</a></li>
                <li><a href="/about">Om</a></li>
            </ul>
        </nav>
    </header>
    <main>
        <h1>Lägg till kurs</h1>
        <div id="error-container">
            <% if(locals.errMessage.length> 0) { %>
                <ul id="error">
                    <% errMessage.forEach((error)=> { %>
                        <li>
                            <%= error %>
                        </li>
                        <%});%>
                </ul>
                <% }%>
        </div>
        <form method="post" action="/" id="course-form">
            <div>
                <label for="code">Kurskod</label>
                <input type="text" name="code" id="code" placeholder="Skriv kurskod..." maxlength="8" value="<%= coursecode %>">
                <br>
            </div>
            <div>
                <label for="name">Kursnamn</label>
                <input type="text" name="name" id="name" placeholder="Skriv kursnamn..." maxlength="75" value="<%= coursename %>">
                <br>
            </div>
            <div>
                <label for="progression">Kursprogression</label>
                <input type="text" name="progression" id="progression" placeholder="Skriv kursprogression..." maxlength="1" value="<%= progression %>">
                <br>
            </div>
            <div>
                <label for="syllabus">Kursplan</label>
                <input type="text" name="syllabus" id="syllabus" placeholder="Skriv kursplan..." maxlength="75" value="<%= syllabus %>">
                <br>
            </div>
            <input type="submit" id="newcourse-button" value="Lägg till kurs">
        </form>
    </main>
</body>

</html>